{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the RTL Terminal main window",
  "windows": ["main*"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:event:default"
  ]
}